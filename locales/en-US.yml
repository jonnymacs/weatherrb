en-US:
  services:
    open_weather_map:
      network:
        get_weather_by_city_name: "Network Error: '%{message}'"
      client:
        get_weather_by_city_name: "Client Error: '%{exception}''"
